version: 2
updates:
  # Maintain dependencies for GitHub Actions
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
    commit-message:
      prefix: "ci"
    labels:
      - "ci-cd"
      - "dependencies"
    open-pull-requests-limit: 5
    
  # Maintain dependencies for NightcapKit's standalone Swift package (NightcapKit/Package.swift only)
  # Note: Main Nightcap project dependencies (SemanticVersion, swift-argument-parser) are in Xcode project-based SPM
  # which Dependabot doesn't support - only standalone Package.swift files are tracked
  - package-ecosystem: "swift"
    directory: "/NightcapKit"
    schedule:
      interval: "weekly"
      day: "monday"
    commit-message:
      prefix: "deps"
    labels:
      - "dependencies"
    open-pull-requests-limit: 5
